import { SortDoctorPipe } from './sort-doctor.pipe';

describe('SortDoctorPipe', () => {
  it('create an instance', () => {
    const pipe = new SortDoctorPipe();
    expect(pipe).toBeTruthy();
  });
});
