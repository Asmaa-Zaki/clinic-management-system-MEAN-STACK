import { SortComponentPipe } from './sort-Component.pipe';

describe('SortComponentPipe', () => {
  it('create an instance', () => {
    const pipe = new SortComponentPipe();
    expect(pipe).toBeTruthy();
  });
});
