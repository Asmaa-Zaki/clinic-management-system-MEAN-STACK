import { SortPatientPipe } from './sort-patient.pipe';

describe('SortPatientPipe', () => {
  it('create an instance', () => {
    const pipe = new SortPatientPipe();
    expect(pipe).toBeTruthy();
  });
});
