import { FilterbyDocnamePipe } from './filterby-docname.pipe';

describe('FilterbyDocnamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbyDocnamePipe();
    expect(pipe).toBeTruthy();
  });
});
