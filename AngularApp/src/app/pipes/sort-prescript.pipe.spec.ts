import { SortPrescriptPipe } from './sort-prescript.pipe';

describe('SortPrescriptPipe', () => {
  it('create an instance', () => {
    const pipe = new SortPrescriptPipe();
    expect(pipe).toBeTruthy();
  });
});
