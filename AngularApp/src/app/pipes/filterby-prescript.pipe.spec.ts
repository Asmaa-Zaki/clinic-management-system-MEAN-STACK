import { FilterbyPrescriptPipe } from './filterby-prescript.pipe';

describe('FilterbyPrescriptPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbyPrescriptPipe();
    expect(pipe).toBeTruthy();
  });
});
