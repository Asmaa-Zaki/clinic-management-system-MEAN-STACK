import { FilterbyPatnamePipe } from './filterby-patname.pipe';

describe('FilterbyPatnamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbyPatnamePipe();
    expect(pipe).toBeTruthy();
  });
});
