export class Employee {
    constructor(public _id:number, public firstName:string, public lastName:string, public email:string, public phone:number, public userName:string, public password: string, public type:string)
    {

    }
}
