import { Doctor } from './doctor';

/*describe('Doctor', () => {
  it('should create an instance', () => {
    expect(new Doctor(_id :number,doctorName:string,SSN:number,
      phone: number,address: String,medicalSpecialty: String,username: String , password: String)).toBeTruthy();
  });
});*/
